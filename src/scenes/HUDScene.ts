import Phaser from "phaser";

export class HUDScene extends Phaser.Scene {
    constructor() {
        super({ key: "HUDScene" });
    }

    create(): void {
        // Placeholder - built in Phase 2
    }
}
